'use client'

import * as TooltipPrimitive from '@radix-ui/react-tooltip'

const TooltipProviders = TooltipPrimitive.Provider

export { TooltipProviders as TooltipProvider }
export default TooltipProviders
